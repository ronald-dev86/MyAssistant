import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jk.ayudante.com',
  appName: 'project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
